export interface User {
  email: string;
}

export interface FeatureCard {
  id: string;
  title: string;
  description: string;
  details: string[];
  icon: string;
}

export interface DemoVideo {
  id: string;
  title: string;
  description: string;
  thumbnail: string;
}